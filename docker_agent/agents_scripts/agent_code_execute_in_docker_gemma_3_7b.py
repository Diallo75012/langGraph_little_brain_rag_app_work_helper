#!/usr/bin/env python3# code from: gemma_3_7b LLM Agent
# User initial request: None
'''This code have been generated by an LLM Agent'''

```python
### API Caller Script
### ================

### Import required libraries
import requests
import json

### Set API endpoint URL
api_url = 'https://api.agify.io/?name=Junko'

### Make API call using requests
response = requests.get(api_url)

### Check API response status code
if response.status_code == 200:
    print("API call successful!")
    print("Response:", response.json())
else:
    print("API call failed!")
    print("Error:", response.text)
```

**Explanation:**

* The script imports the `requests` library for API requests and the `json` library for working with JSON data.
* It sets the API endpoint URL to `https://api.agify.io/?name=Junko`.
* It sends a GET request to the API endpoint using the `requests.get()` method.
* It checks the API response status code and prints the response data or an error message.

**Note:**

* Replace `Junko` with the actual name of your friend.
* The API response will contain the age of the friend.
* Make sure to install the `requests` library if you haven't already, by running `pip install requests` in your terminal.